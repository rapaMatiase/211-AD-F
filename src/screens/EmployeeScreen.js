import React from "react";
import {
    Button,
    Stack,
    Flex,
    NativeBaseProvider,
  } from "native-base";

const EmployeeScreen = () => {

    return (
        <NativeBaseProvider>
            
        </NativeBaseProvider>
    );
}